CREATE DATABASE voluntree_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'voluntree_user'@'localhost' IDENTIFIED BY 'senha_segura';
GRANT ALL PRIVILEGES ON voluntree_db.* TO 'voluntree_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;

waitress-serve --host=0.0.0.0 --port=8000 voluntree.wsgi:application




worker_processes 1;

events {
    worker_connections 1024;
}

http {
    include mime.types;
    default_type application/octet-stream;
    sendfile on;
    keepalive_timeout 65;
    
    server {
        listen 80;
        server_name localhost;
        
        location / {
            proxy_pass http://127.0.0.1:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
        
        location /static/ {
            alias C:/sites/voluntree-opsystem/staticfiles/;
        }
    }
}

STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')

C:\nssm\win64\nssm.exe install VoluntreeDjango
Start-Service VoluntreeDjango
C:\nssm\win64\nssm.exe install Nginx
Start-Service Nginx
Set-Service -Name VoluntreeDjango -StartupType Automatic
Set-Service -Name Nginx -StartupType Automatic
New-NetFirewallRule -DisplayName "Allow HTTP" -Direction Inbound -Protocol TCP -LocalPort 80 -Action Allow
Get-NetFirewallProfile